describe("Try to export a natif project to aws", () => {
  it("should be able to create new url project", () => {
    cy.visit(`http://localhost:6001`)
  })

  it("should be able to go to the import page", () => {})

  it("should be able to to click on export aws", () => {})

  it("should be able to see the new project in export dialog", () => {})
})
