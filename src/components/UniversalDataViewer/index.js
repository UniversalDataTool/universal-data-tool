import React from "react"
import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles({})

export const UniversalDataViewer = ({ oha }) => {
  const c = useStyles()
  return <div>universal data viewer</div>
}

export default UniversalDataViewer
